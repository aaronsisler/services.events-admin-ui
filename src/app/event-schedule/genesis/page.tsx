import { EventScheduleForm } from "./event-schedule-form";

function GenesisEventSchedule() {
  return (
    <main>
      <br />
      <br />
      <div>
        This will create the event schedule and then navigate to add Events as
        Scheduled Events to the Event Schedule.
      </div>
      <br />
      <EventScheduleForm />
    </main>
  );
}

export default GenesisEventSchedule;
